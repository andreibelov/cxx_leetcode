#pragma once

#include <vector>
#include <string>
#include <utility>
#include <iostream>
#include <algorithm>

struct Solution {
	// TODO: adapt signature to the problem
	int solve(const std::vector<int>& nums);
};
